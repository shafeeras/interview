import React from "react";
import PropTypes from "prop-types";
import { Layout } from "./styled";

const NotFount = props => {
  return <Layout>The page you are looking for does not exist!</Layout>;
};

NotFount.propTypes = {};

export default NotFount;
