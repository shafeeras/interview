// common reducer goes here

const initialState = {};

const commonReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default commonReducer;